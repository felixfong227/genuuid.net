---
import UuidApp from '../components/UuidApp';
import BaseLayout from '../layouts/BaseLayout.astro';
export const prerender = true;

const title = 'UUID v7 Generator (Time-Ordered) | genuuid.net';
const description =
    'Generate UUID version 7 (time-ordered) locally in your browser. Great for sortable IDs. Copy a single UUID or bulk-generate up to 500 at once.';

const site = Astro.site?.toString() ?? 'https://genuuid.net';
const pageUrl = new URL(Astro.url.pathname, site).toString();

const jsonLd = {
    '@context': 'https://schema.org',
    '@graph': [
        {
            '@type': 'WebSite',
            '@id': `${site}#website`,
            url: site,
            name: 'genuuid.net',
            inLanguage: 'en',
        },
        {
            '@type': 'WebPage',
            '@id': `${pageUrl}#webpage`,
            url: pageUrl,
            name: title,
            description,
            isPartOf: { '@id': `${site}#website` },
            inLanguage: 'en',
        },
        {
            '@type': 'WebApplication',
            '@id': `${pageUrl}#app`,
            name: 'UUID v7 Generator',
            url: pageUrl,
            description,
            applicationCategory: 'DeveloperApplication',
            operatingSystem: 'Any',
            offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
        },
    ],
};
---

<BaseLayout title={title} description={description} jsonLd={jsonLd}>
  <main
    id="main"
    class="relative mx-auto flex min-h-screen max-w-4xl flex-col gap-8 px-4 py-12 sm:px-6 md:gap-10 md:py-16 lg:py-20"
  >
    <header class="animate-float-up space-y-4">
      <div class="space-y-3">
        <p class="font-mono text-xs uppercase tracking-widest text-white/40">
          genuuid.net
        </p>
        <h1 class="text-3xl sm:text-4xl font-semibold tracking-tight text-white">
          UUID v7 Generator (Time-Ordered)
        </h1>
        <p class="text-white/70 leading-relaxed max-w-prose">
          UUID v7 is a modern UUID format designed for time-ordered IDs. It’s a
          great choice when you want mostly-random identifiers that also sort well
          in databases and logs.
        </p>
      </div>

      <div class="flex flex-wrap items-center gap-2 font-mono text-xs text-white/50">
        <span class="uppercase tracking-widest">More:</span>
        <a
          href="/"
          class="rounded-md border border-white/10 bg-white/5 px-2 py-1 text-white/70 hover:text-emerald-200 hover:border-emerald-400/40 transition-colors"
        >
          All versions
        </a>
        <a
          href="/v4/"
          class="rounded-md border border-white/10 bg-white/5 px-2 py-1 text-white/70 hover:text-emerald-200 hover:border-emerald-400/40 transition-colors"
        >
          UUID v4
        </a>
        <a
          href="/v1/"
          class="rounded-md border border-white/10 bg-white/5 px-2 py-1 text-white/70 hover:text-emerald-200 hover:border-emerald-400/40 transition-colors"
        >
          UUID v1
        </a>
      </div>
    </header>

    <UuidApp client:load initialVersion="v7" />
  </main>
</BaseLayout>

